import { type Metadata } from 'next'

import { Providers } from '@/app/providers'
import { Layout } from '@/components/Layout'

import '@/styles/tailwind.css'

export const metadata: Metadata = {
  title: {
    template: '%s - R.B. Sharma Commerce Classes',
    default:
      'R.B. Sharma Commerce Classes - Best Commerce Coaching in Chandigarh',
  },
  description:
    'Join R.B. Sharma Commerce Classes — Chandigarh’s most trusted coaching for Class 11, 12, and B.Com. Get expert guidance in Accountancy, Business Studies, and Economics.',
  alternates: {
    types: {
      'application/rss+xml': `${process.env.NEXT_PUBLIC_SITE_URL}/feed.xml`,
    },
  },
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en" className="h-full antialiased" suppressHydrationWarning>
      <body className="flex h-full bg-zinc-50 dark:bg-black">
        <Providers>
          <div className="flex w-full">
            <Layout>{children}</Layout>
          </div>
        </Providers>
      </body>
    </html>
  )
}
